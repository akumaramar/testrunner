<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match dotnet.**>
  @type file
  path /fluentd/log/dotnet
  append true
  <buffer>
    timekey 1d
    timekey_use_utc true
    timekey_wait 10s
  </buffer>
  <format>
    @type json
  </format>
</match>
