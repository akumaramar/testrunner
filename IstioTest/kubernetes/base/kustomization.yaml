apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# Istio Gateway and routing
- gateway.yaml

# Services and Deployments
- product-service.yaml
- order-service.yaml
- customer-service.yaml

commonLabels:
  environment: demo
  managed-by: kustomize

namespace: istio-demo
